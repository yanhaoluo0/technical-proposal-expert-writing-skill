# 软件工程文档编写规范

写任何 Word 类文档前必读。表格类（xlsx）和会议纪要按对应 reference，但仍遵守术语一致、禁止编造。

## 封面

按下列字段输出，不要改名：

| 字段 | 规则 |
|---|---|
| 文档状态 | 三选一勾选：草稿 / 修订 / 发布。新建默认「草稿」 |
| 保密级别 | 未知则「待确认」 |
| 文档编号 | 未知则「待确认」，不要编造流水号 |
| 管理部门 | 未知则「待确认」 |
| 修订年月 | `YYYY年MM月` |
| 版本号 | 初稿 `V0.1` 或 `V1.0`（用户指定则从其指定） |
| 项目名称 | 居中主标题之一，用用户给出的项目名 |
| 文档名称 | 与类型全称一致，如「需求规格说明书」 |
| 修订人 / 审核人 / 批准人签字 | 未知写「待确认」；日期格式 `YYYY-MM-DD` |

Markdown 可用如下块表达封面（列名保持一致）：

```markdown
| 文档状态 | 保密级别 | 文档编号 | 管理部门 | 修订年月 | 版本号 |
|---|---|---|---|---|---|
| [√]草稿 [ ]修订 [ ]发布 | 待确认 | 待确认 | 待确认 | 2026年08月 | V0.1 |

| 修订人签字 | 审核人签字 | 批准人签字 |
|---|---|---|
| 待确认 | 待确认 | 待确认 |
| 日期：YYYY-MM-DD | 日期： | 日期： |
```

## 变更履历

| 序号 | 变更日期 | 版本 | 变更位置 | 变更原因 | 修订人 | 审核人 | 批准人 |
|---|---|---|---|---|---|---|---|
| 1 | YYYY-MM-DD | V0.1 | 全文 | 建立初稿 | 待确认 | 待确认 | 待确认 |

**变更原因只允许三个值：**

- `建立初稿` — 第一次成文
- `内容修订` — 已有版本上改内容
- `正式发布` — 状态改为发布时

不要写「更新」「微调」「根据会议修改」等其他原因词；具体改动放在「变更位置」。

## 文档介绍（固定五节）

模板含「文档介绍」时，五节都要写，并用下面句式（可补项目具体信息，但不要删掉目的/范围/对象）：

### 编写目的

> 本文档旨在说明【项目名称】的【文档对象，如功能需求 / 可行性 / 测试安排】，作为【后续活动，如设计开发 / 立项决策 / 测试执行】的依据。

禁止只写「说明文档的编写目的」这种模板占位句。

### 文档范围

> 本文档覆盖【列出将写的主要章节或模块】。不包括【明确边界，如详细代码实现 / 商务合同条款】。

### 读者对象

> 本文档主要面向【角色列表，如项目经理、开发、测试、客户代表】。

### 术语与缩写解释

必须用表，列名固定：

| 术语或缩写 | 解释 |
|---|---|
| 示例：SRS | 软件需求规格说明书 |

正文里出现的项目特有缩写、模块名、外部系统名，首次出现前必须进此表。

### 参考资料

必须用表，列名固定：

| 序号 | 文档名称 | 文档编号 | 版本 | 发布日期 |
|---|---|---|---|---|
| 1 | 待确认 | 待确认 | 待确认 | 待确认 |

有真实依据就填真实依据（标准号、前序文档）。不要伪造国标编号。

## 正文结构

- 章节编号：`1`、`1.1`、`1.1.1`，与模板目录一致
- 先写规定小节，再按用户内容往下展开；不要把用户零散素材堆在最前面当「摘要」冒充结构
- 附录只放补充材料（签字页、图表索引、原始数据），不替代正文章节

## 术语

- 同一事物只用一个名称（选定「告警」就不要混用「报警」「预警」，除非术语表声明三者关系）
- 角色名与「用户类别」表保持一致
- 功能名与编号一旦确定，后文、用例、提测单必须复用同一套编号

## 图表

- 图：`图{章号}-{序号}`，如 `图3-1 当前业务流程图`
- 表：`表{章号}-{序号}`，如 `表2-1 术语与缩写解释`
- 正文引用：「见图3-1」「见表2-1」
- 有图无标题、有表无表头，视为不合格

## 可验证表述

| 禁止 | 改为 |
|---|---|
| 性能较好、尽快、基本满足 | 并发 ≥ 200、响应 ≤ 2s、T+1 完成 |
| 支持多种浏览器 | Chrome 120+、Edge 120+（以项目为准） |
| 系统较为稳定 | 连续运行 7×24 小时、故障恢复 ≤ 30 分钟 |
| 部分功能 | 列出模块名与编号 |

未知指标写「待确认（需量化）」，不要用形容词顶替。

## 审批链

```
编写 → 自检（本 skill 检查清单）→ 审核 → 批准
```

Agent 负责编写和自检。审核人/批准人未知时保持「待确认」，不要代签。

## Markdown 与模板格式

不必复现「初号字体、居中加粗」等 Word 样式；必须复现：**字段、章节、表格列、编号规则、固定句式**。用户要 docx 时，再按模板样式填入。
